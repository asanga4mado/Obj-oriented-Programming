package ROT13Cipher;

im

public class ROT13Cipher {

    public static void main(String[] args) {

        // ROT13 cipher is a very simple encryption cipher that rotates the alphabet by 13 letters to change
        // "A" TO "N", "B" to "0", "C" to "P", "X" TO "K", "Y" to "L", "Z" to "M" etc

        //e.g
        //"secret" would be encrypted to "frperg"

        //Create a program that takes a single word lowercase letters, stored as a constant variable, and outputs it as
        //a ROT13 encrypted version.



    }
}
